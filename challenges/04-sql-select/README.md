# 04 - Banco de Dados (MySQL / SQL)

Script com criação da tabela `users`, inserção dos dados de exemplo e as queries pedidas de listagem por data de criação e contagem de usuários por mês.

## Pré-requisitos

- Um servidor MySQL

## Como rodar

Copie e cole o conteúdo de `queries.sql` em algum cliente MySQL (DBeaver, MySQLWorkbench...) e execute